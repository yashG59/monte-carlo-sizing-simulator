[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "montecarlo-trading"
version = "0.1.0"
description = "Elite Monte Carlo quant simulator for strategy sizing tradeoffs"
requires-python = ">=3.10"
